{
  "name": "@mungojam/use-persisted-state",
  "publishConfig": {
    "tag": "latest"
  },
  "version": "0.9.0",
  "description": "A custom React Hook that provides a multi-instance, multi-tab/browser shared and persistent state.",
  "main": "dist/use-persisted-state.js",
  "module": "dist/use-persisted-state.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "source": "src/index.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/donavon/use-persisted-state.git"
  },
  "scripts": {
    "lint": "yarn tsdx lint",
    "test": "yarn jest --verbose --coverage --silent",
    "test:watch": "yarn jest --watch --runInBand --silent",
    "build": "yarn tsdx build"
  },
  "keywords": [
    "react-hooks",
    "hooks",
    "react",
    "utils",
    "lib",
    "persistent",
    "localstorage"
  ],
  "author": "mungojam (forked from donavon)",
  "devDependencies": {
    "@babel/preset-react": "^7.12.5",
    "@types/react": "^16.9.56",
    "jest": "^23.6.0",
    "react": "^16.14.0",
    "tsdx": "^0.14.1",
    "tslib": "^2.0.3",
    "typescript": "^4.0.5"
  },
  "peerDependencies": {
    "react": "^16.14.0"
  },
  "jest": {
    "coverageThreshold": {
      "global": {
        "branches": 0,
        "functions": 0,
        "lines": 0,
        "statements": 0
      }
    }
  },
  "dependencies": {
    "use-typed-event-listener": "^3.0.0"
  }
}
